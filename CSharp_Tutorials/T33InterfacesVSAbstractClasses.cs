using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace CSharp_Tutorials
{
   public abstract class T33InterfacesVSAbstractClasses
    {
       // abstract classes may have the implementations of the methods.
       // It can have the ACCESS MODIFIER 
       // All method considered as a private by default
      // A class cannot implements multiple classes. NO MULTIPLE INHERITANCE
   // it can have fields
   }
   public interface checkDifferencebtwabsClass
   {
       // it have no implementations
       // all method considered as a PUBLIC by default
       // it not have any fields
   }
}
